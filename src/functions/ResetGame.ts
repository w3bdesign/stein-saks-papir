import { useContext } from 'react';

import ScoreBoardContext from '../context/ScoreBoardContext';

function ResetGame() {
  // const [score, setScore] = useContext(ScoreBoardContext);
  // console.log('Reset game ...');
  // setScore(null);
  // console.log(Weapon);
}

export default ResetGame;
